# Foundation Checklist

- Shared auth and dashboard route shells exist under `src/app`.
- `src/lib/supabase` owns browser, server, and middleware helpers.
- `src/components/layout` owns sidebar, header, role badge, and breadcrumbs.
- Root error, not-found, and loading entry points are ready for later hardening.

